import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#4829B2',
            darkGrey: '#212121',
        },
        secondary: {
            main: "#fff"
        }

    }
});

export default theme;